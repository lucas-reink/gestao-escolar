import React from 'react';
import './style.css';

const Dashboard = () => {
    

    return (
        <div className="dashboard">
            <h1>Dashboard</h1>
            <p>
                This is the dashboard where administrators can manage students, teachers, and classes.
            </p>
           
        </div>
    );
};

export default Dashboard;
